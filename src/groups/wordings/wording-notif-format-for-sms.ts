export const wordingNotifFormatForSms = (username: string): string => {
  const message = `Hai, ${username}!\n\nNo HP Anda berhasil diperbaharui, Anda dapat login pada aplikasi eFOOD menggunakan No HP ini.!`;
  return message;
};
